fastapi
uvicorn[standard]
faster-whisper
numpy
segno            # QR codes, rendered locally so they survive the network dropping
pypdf            # PDF text only. DOCX is a zip of XML and needs no library.

# Not listed, on purpose:
#   auth        hashlib.scrypt, hmac and secrets are stdlib and are the parts
#               that matter. See backend/accounts.py.
#   database    sqlite3 is stdlib, and the whole catalog is a few thousand rows.
#   http        urllib does every outbound call here. requests would be nicer to
#               read and would not do anything urllib does not.

# The voice, cloned from a recording the customer supplies. MIT licensed, so
# shipping it inside a bundle is fine, and it clones zero-shot from about ten
# seconds of reference audio — no training run and no per-voice setup.
#
# Apple Silicon only, deliberately. It runs on Metal there, which is the whole
# reason this is a Mac application rather than a container; elsewhere the browser
# synthesiser stays the voice, which is what every install had until now.
chatterbox-tts; platform_system == "Darwin" and platform_machine == "arm64"
